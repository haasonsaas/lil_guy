name: Comprehensive Quality Checks

on:
  pull_request:
    types: [opened, synchronize, reopened]
  push:
    branches: [main, develop]
  workflow_dispatch:

permissions:
  contents: read
  issues: write
  pull-requests: write

jobs:
  quality-checks:
    runs-on: blacksmith-4vcpu-ubuntu-2404

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Install Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: 'latest'

      - name: Cache dependencies
        uses: useblacksmith/cache@v5
        with:
          path: ~/.bun/install/cache
          key: ${{ runner.os }}-bun-${{ hashFiles('**/bun.lockb') }}
          restore-keys: |
            ${{ runner.os }}-bun-

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Run all quality checks
        id: quality-checks
        run: bun run check:all
        env:
          VITE_RESEND_API_KEY: dummy
          VITE_EMAIL_TO: dummy
          VITE_EMAIL_FROM: dummy

      - name: Comment PR with results
        uses: actions/github-script@v7
        if: github.event_name == 'pull_request'
        with:
          script: |
            const status = '${{ steps.quality-checks.outcome }}' === 'success' ? '✅ All quality checks passed!' : '❌ Some quality checks failed';
            const output = `
            ## 🔍 Comprehensive Quality Check Results

            ${status}

            The comprehensive quality check includes:
            - TypeScript compilation
            - ESLint code quality 
            - Prettier formatting
            - Markdown linting
            - Spell checking
            - SEO validation
            - Link checking
            - Bundle size validation
            - Analytics & Service Worker tests

            Check the workflow logs for detailed results.
            `;

            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: output
            });
